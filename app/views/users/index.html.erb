<h1><%= t('activerecord.models.user') %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user.id) %>
    </li>
    <% end %>
</ul>

<%= paginate @users %>
